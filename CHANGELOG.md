# Changelog

本文件记录 xiangqi-model-match 的版本变更。版本号遵循「主.次.修」：
大版本=架构级重写，次版本（+0.1）=新特性/行为修正，修订号=纯 bugfix。

## v1.1.2（2026-08-22）

### 新增

- **结构化复盘记录 `replay.jsonl`**（JSONL，逐行 JSON）：每步记录每次模型调用的
  原始输出（`raw`，含思考链）、发送提示（`prompt`）、API 耗时（`api_sec`）、
  提取走法与裁判校验结论（`valid` / `invalid` / `error` / `resign`）、局次/手数/
  执方/模型。补齐 v1.0 重构时丢失的基础版 xiangqi-arena `match.jsonl` 能力——
  赛后逐手复盘「模型在哪一步想错、裁判如何驳回」不再依赖人类日志。
  `init` 时自动清空，与当前系列赛对齐。

## v1.1.1（2026-08-21）

代码审查（27B 自我博弈长局跑批期间）发现的遗留 bug 修复 + 测试套件与比赛级时限。

### 修复

- **`arena_log.md` 比分表从不更新**（v1.0 遗留死代码）：`refresh_log_header()` 是空函数，
  日志头部在 `init_log` 写死 `0:0` 后从不刷新，整场对局结束比分仍停在初始值。现实现
  `_write_log_head` 共用头部渲染 + 每局结束后重建头部（保留走子记录），比分表跟随
  `score`/`games` 实时更新。
- **`parse_move` 思考标签硬编码**：原实现只认 Qwen 服务端特有的 `</think:6124c78e>`
  闭合标记，换任何其他模型（`</think>`、`</reasoning>`、`</output>`）都匹配不到，
  思考链里提到的坐标会被误解析为走法（正则取最后一个匹配，覆盖真正的落子）。
  改为按常见闭合标签切段、从后向前找含坐标的段，兼容「答案在标签后」与
  「答案包在 `<output>` 内」两类形态；无闭合标签（思考被截断）退化取整段最后坐标。
- **`_after_game` G2 换先逻辑死 `.get`**：`st['games'][0].get('red_idx', ...)` 永远走
  default 分支（games 记录根本没有 `red_idx` 字段），靠 `current` 尚未被覆盖的时序
  碰巧正确，极脆弱。现 games 记录显式落 `red_idx`/`black_idx`，换先逻辑有了可靠依据。
- **`detect_resign` 冗余分支**：`'我认输'` 是 `'认输'` 的超集，删除。

### 新增

- **比赛级墙钟上限 `run --max-hours H`**：解决超长对局（如 27B 自我博弈单局 10+ 小时）
  无整体时限的问题；`--max-hours` 为软停止（状态正常落盘，重跑可续跑），
  与单局手数上限 `--max-plies` 互补。
- **测试套件 `tests/`**（纯标准库 unittest，27 例）：
  `tests/test_referee.py` 覆盖蹩马腿 / 塞象眼 / 炮架 / 将死 / 困毙 / 子力不足 /
  将帅对脸（含造成对脸的走法判非法）/ 基础走子校验；`tests/test_arena.py` 覆盖
  思考标签泛化与认输检测。运行：`python -m unittest discover tests`。
- README 命令参考与文件表同步更新；终局判定表新增「长将/长捉」**未实现**标注。

### 已知限制

- **连续将军（长将）判负未实现**：机械裁判只判定将死/困毙/三次重复/自然限着/子力
  不足，不识别"一方连续将军须变着、否则判负"（中国象棋正式规则）。LLM 对局中长将
  罕见，但作为公开的「机械裁判」应如实标注；出现时可人工按规则裁决。

## v1.1.0（2026-08-21）

27B 自我博弈实测（海光 BW3000 单卡，vllm-dcu 后端）暴露的两个行为缺陷修复 + 思考模式可控。

### 修复

- **犯规计数恢复「连续」语义**：此前 `faults` 计数只增不清零，实际行为是"累计 3 次非法走子判负"，与 README 声称的"连续 3 次"不符——偶尔一次非法（被纠错后继续正常下）也会累积，实测 3 局均在约 10 手时被误判终局。现在走子成功后清零该方计数。
- **openai 选手默认开启思考链**：vLLM 后端会**严格执行** `enable_thinking=False`（transformers 自建服务则忽略该参数），关闭思考导致 27B 棋力骤降（红黑坐标混淆、来回穿梭成和）。默认改为 `enable_thinking=True`，且 `max_tokens` 1024 → 4096（预算太小会被截断在思考中途导致解析失败）。

### 新增

- **思考模式可按选手配置**：选手格式新增可选第 6 段 `think=on|off`（默认 `openai=on / ollama=off`），支持混合对照实验（如开思考 vs 关思考 A/B）。
- `arena.py version` 命令与 `__version__` 常量。
- 本 CHANGELOG.md；README 新增「思考模式与后端兼容」小节（vLLM / transformers / Ollama 三种后端行为对照表）。

## v1.0.0（2026-08-20）

首个重构版：`arena.py` 自动 runner 替换旧文件式中继范式。

- 三种选手类型（openai / ollama / stdio），BO_N 系列赛制（每局交换先后手）
- 机械裁判 `referee.py`（走法生成 / 将死 / 困毙 / 三次重复 / 自然限着 / 子力不足 / 认输 / 犯规判负）
- 断点续跑（`match_state.json` + `arena_log.md`），模型调用内部 3 次重试
- 纯标准库，零第三方依赖
